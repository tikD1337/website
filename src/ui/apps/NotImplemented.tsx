import { APPS, type AppId } from '../../store/windows'

/**
 * Заглушка для приложений, которых ещё нет.
 *
 * Иконки проводника, браузера и параметров стоят на десктопе, потому что
 * без них рабочий стол выглядит бутафорией. Но содержимого у них не
 * будет, пока не появится сценарий, которому они нужны: делать окно
 * без потребителя — работа впустую.
 */
export function NotImplemented({ id }: { id: AppId }) {
  return (
    <div className="stub">
      <p>{APPS[id].title} появится вместе со сценарием, которому понадобится.</p>
      <p className="sub">
        Сейчас доступны командная строка, просмотр событий, службы и
        диспетчер устройств.
      </p>
    </div>
  )
}
